_schema-version: "3.2"
ID: comviatrismaterialmaster
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: comviatrismaterialmaster-destination-content
  type: com.sap.application.content
  requires:
  - name: mm-destination-service
    parameters:
      content-target: true
  - name: comviatrismaterialmaster-repo-host
    parameters:
      service-key:
        name: comviatrismaterialmaster-repo-host-key
  - name: comviatrismaterialmaster-uaa
    parameters:
      service-key:
        name: comviatrismaterialmaster-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: comviatrismaterialmaster_html_repo_host
          ServiceInstanceName: comviatrismaterialmaster-html5-srv
          ServiceKeyName: comviatrismaterialmaster-repo-host-key
          sap.cloud.service: comviatrismaterialmaster
        - Authentication: OAuth2UserTokenExchange
          Name: comviatrismaterialmaster_uaa
          ServiceInstanceName: comviatrismaterialmaster-xsuaa-srv
          ServiceKeyName: comviatrismaterialmaster-uaa-key
          sap.cloud.service: comviatrismaterialmaster
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: comviatrismaterialmaster-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: comviatrismaterialmaster-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - comviatrismaterialmaster.zip
      name: comviatrismaterialmaster
      target-path: resources/
- name: comviatrismaterialmaster
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: mm-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: mm-destination-service
    service-plan: lite
- name: comviatrismaterialmaster-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: comviatrismaterialmaster-xsuaa-srv
    service-plan: application
- name: comviatrismaterialmaster-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: comviatrismaterialmaster-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
